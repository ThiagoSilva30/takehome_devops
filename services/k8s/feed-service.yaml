apiVersion: v1
kind: Service
metadata:
  
  labels:
    app: feed
  name: feed-service
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: feed
  type: NodePort
status:
  loadBalancer: {}